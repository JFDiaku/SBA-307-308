function toggleDropdown() {
  const dropdown = document.getElementById('dropdown');
  const dropButton = document.getElementById('drop-button');
  dropdown.classList.toggle('show');
  dropButton.classList.toggle('show');
}